This CoolProp.oct module is built using SWIG, and is built for octave 3.6.1 for windows built with Visual Studio.  Other versions may work, but no guarantee

You can run the example by running the command::

	octave sample_code.m

at the command prompt in the same folder as the files